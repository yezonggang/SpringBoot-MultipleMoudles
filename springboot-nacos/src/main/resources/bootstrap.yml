spring:
  application:
    name: nacos-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 #nacos服务地址
      discovery:
        namespace: df843b7b-1ff4-495a-baef-6b1296c531ed
        enabled: true #默认值
      config:
        file-extension: yml #文件后缀名
        namespace: df843b7b-1ff4-495a-baef-6b1296c531ed
        enabled: true #默认值
        refresh-enabled: true # 运行自动刷新配置
        prefix: nacos-service #配置文件前缀
t1: test
#userservice: # 负载均衡规则
#  ribbon:
#    NFLoadBalancerRuleClassName: com.alibaba.cloud.nacos.ribbon.NacosRule
#ribbon:
#  eager-load:
#    enabled: true # 开启饥饿加载
#    clients: # 指定饥饿加载的服务名称
#      - userservice